import { NotFound } from "@/components/not-found";

const PageNotFound = () => {
  return (
    <NotFound
      title="Page not found"
      description="The page you are looking for does not exist. Please check the URL, or head"
    />
  );
};

export default PageNotFound;
