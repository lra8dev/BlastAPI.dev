# 1. Client Environment Variables (apps/web/.env.local)

# AuthJS Secret
AUTH_SECRET=""

# Google OAuth
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# GitHub OAuth
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# Server
NEXT_PUBLIC_SERVER_URL="http://localhost:5001"

# Database (required for Prisma adapter in Auth.js)
DATABASE_URL="postgres://daniel:<password>@ep-mute-rain-952417-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"


# 2. Server Environment Variables (apps/server/.env)

# Server
PORT=5001
SERVER_VERSION="1.0.0"
NODE_ENV=development # TODO: update to production before deployment

# Redis
REDIS_HOST="redis-"
REDIS_PASSWORD=""
REDIS_PORT=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000 # 1 minute
RATE_LIMIT_MAX_REQUESTS=100

# Load Testing
MAX_CONCURRENT_TESTS=5
MAX_VIRTUAL_USERS=1000
MAX_TEST_DURATION=480

# Client
CLIENT_URL="http://localhost:3000"


# 3. DB package environment variables (packages/db/.env)

# Connect to Neon with pooling (used by Prisma Client via the adapter).
DATABASE_URL="postgres://daniel:<password>@ep-mute-rain-952417-pooler.us-east-2.aws.neon.tech/neondb?sslmode=require"

# Direct connection to the database used by the Prisma CLI.
DIRECT_URL="postgres://daniel:<password>@ep-mute-rain-952417.us-east-2.aws.neon.tech/neondb"